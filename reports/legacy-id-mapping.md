# LEGACY ID MAPPING MATRIX

**Date:** 2026-07-23  

## Mapping Overview
All legacy runtime IDs in `src/lib/grant-writer/deterministic/registry.ts` have been cross-checked against canonical IDs in `ontology/*.yaml`.

- **Identical IDs (Class a):** 100% mapped without alias requirement.
- **Variant IDs (Class b):** Mapped via `aliases[]` property in canonical YAML objects.
- **Unmapped IDs (Class c):** 0 unmapped IDs remaining.
